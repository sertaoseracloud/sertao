---
phase: 08-share-og-about
plan: 03
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - src/lib/consts.ts
  - src/pages/sobre.astro
autonomous: false
requirements:
  - REQ-SOBRE-01
  - REQ-VAL-01

must_haves:
  truths:
    - "/sobre page renders at build time without error"
    - "Page contains author name (Cláudio Rapôso / Cláudio Filipe Lima Rapôso), bio narrative, thesis section 'Por que O Sertão será Cloud?', and social links row"
    - "Social links present: GitHub, LinkedIn, X, email (all four from D-16)"
    - "LinkedIn URL is defined in consts.ts SOCIAL object"
    - "Author photo img element present (renders even if /images/author.jpg is absent at build)"
    - "OG image and twitter:card meta tags present in /sobre HTML (via BaseLayout SEO)"
  artifacts:
    - path: "src/pages/sobre.astro"
      provides: "/sobre page — author identity page"
      contains: "Por que"
    - path: "src/lib/consts.ts"
      provides: "SOCIAL object with linkedin key"
      contains: "linkedin"
  key_links:
    - from: "src/pages/sobre.astro"
      to: "src/lib/consts.ts"
      via: "import { SOCIAL, AUTHOR } from '../lib/consts'"
      pattern: "SOCIAL\.linkedin"
    - from: "src/pages/sobre.astro"
      to: "src/layouts/BaseLayout.astro"
      via: "BaseLayout title + description props"
      pattern: "title=\"Sobre"
---

<objective>
Add LinkedIn to consts.ts SOCIAL object and create the /sobre page with author photo, bio, thesis section, and social links.

Purpose: `/sobre` establishes authorial identity — who wrote this, why the blog exists, and how to reach the author. The LinkedIn key in consts.ts enables the social links row and may be reused by Footer in a future phase.

Output: modified `src/lib/consts.ts` (linkedin key), new `src/pages/sobre.astro` (full page). Includes a checkpoint for the author to add their photo to `public/images/author.jpg`.
</objective>

<execution_context>
@C:/Repo/blog_sertao/.claude/get-shit-done/workflows/execute-plan.md
@C:/Repo/blog_sertao/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Repo/blog_sertao/.planning/ROADMAP.md
@C:/Repo/blog_sertao/.planning/STATE.md
@C:/Repo/blog_sertao/.planning/phases/08-share-og-about/08-CONTEXT.md
@C:/Repo/blog_sertao/.planning/phases/08-share-og-about/08-UI-SPEC.md
@C:/Repo/blog_sertao/.planning/phases/08-share-og-about/08-PATTERNS.md

<interfaces>
<!-- Key types and contracts extracted from codebase. Use directly — no exploration needed. -->

From src/lib/consts.ts (current full file):
```typescript
export const SITE_URL = 'https://sertaoseracloud.com';
export const SITE_TITLE = 'O Sertão será Cloud';
export const SITE_DESCRIPTION = 'Blog sobre cloud computing em português — O Sertão será Cloud.';

export const AUTHOR = {
  name: 'Cláudio Filipe Lima Rapôso',
  handle: 'sertaoseracloud',
};

export const SOCIAL = {
  github: 'https://github.com/sertaoseracloud',
  devto: 'https://dev.to/sertaoseracloud',
  rss: '/rss.xml',
};
```

From src/pages/privacidade.astro (the direct template for sobre.astro):
```astro
---
import BaseLayout from '../layouts/BaseLayout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
---

<BaseLayout title="..." description="...">
  <Header slot="header" />
  <div class="stage">
    <article class="prose" style="max-width:68ch;margin:64px auto;">
      ...
    </article>
  </div>
  <Footer slot="footer" />
</BaseLayout>
```

CSS classes available from global.css Phase 8 section (added in plan 02):
- `.sobre-photo` — 160×160, object-fit:cover, float:right desktop / block centered mobile
- `.sobre-social-row` — flex row, gap 24px, flex-wrap
- `.sobre-social-link` — Chakra Petch 13px, uppercase, color --texto-secundario, hover --nucleo-eletrico

AUTHOR.name = 'Cláudio Filipe Lima Rapôso'
AUTHOR.handle = 'sertaoseracloud'
Email = 'engcfraposo@gmail.com' (from STATE.md session context + CONTEXT.md D-16)
LinkedIn URL pattern per PATTERNS.md: 'https://www.linkedin.com/in/sertaoseracloud'
  (use this URL in consts.ts; author can update to their actual profile if handle differs)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add LinkedIn to consts.ts and create /sobre page</name>
  <files>src/lib/consts.ts, src/pages/sobre.astro</files>
  <read_first>
    - C:/Repo/blog_sertao/src/lib/consts.ts — current SOCIAL object to add linkedin key
    - C:/Repo/blog_sertao/src/pages/privacidade.astro — full page structure to replicate as template
    - C:/Repo/blog_sertao/.planning/phases/08-share-og-about/08-UI-SPEC.md — /sobre page spec (layout, copywriting contract, social links row, photo pattern)
  </read_first>
  <action>
**Part A — Modify `src/lib/consts.ts`:**

Add `linkedin` key to the SOCIAL export object. The updated SOCIAL object:

```typescript
export const SOCIAL = {
  github: 'https://github.com/sertaoseracloud',
  devto: 'https://dev.to/sertaoseracloud',
  rss: '/rss.xml',
  linkedin: 'https://www.linkedin.com/in/sertaoseracloud',
};
```

Note: This LinkedIn handle follows the same convention as all other social handles in this file. If the author uses a different LinkedIn slug, they can update this value — but DO NOT leave it as a placeholder. Use `sertaoseracloud` per the established convention.

**Part B — Create `src/pages/sobre.astro`:**

Follow the `privacidade.astro` structure exactly: BaseLayout + Header slot + div.stage + article.prose + Footer slot. No `export const prerender` needed (SSG default).

CSS classes `.sobre-photo`, `.sobre-social-row`, `.sobre-social-link` are defined in global.css Phase 8 section (added in plan 02). Use them directly.

Full page content:

```astro
---
import BaseLayout from '../layouts/BaseLayout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import { AUTHOR, SOCIAL } from '../lib/consts';

const socials = [
  { href: SOCIAL.github, label: 'GitHub', external: true },
  { href: SOCIAL.linkedin, label: 'LinkedIn', external: true },
  { href: `https://x.com/${AUTHOR.handle}`, label: 'X / Twitter', external: true },
  { href: 'mailto:engcfraposo@gmail.com', label: 'Email', external: false },
];
---

<BaseLayout
  title="Sobre — Cláudio Rapôso"
  description="Engenheiro de software especializado em cloud computing. Criador do blog O Sertão será Cloud — sobre AWS, Terraform e arquitetura de sistemas em português."
>
  <Header slot="header" />

  <div class="stage">
    <article class="prose" style="max-width:68ch;margin:64px auto;">
      <h1>Sobre</h1>

      <p style="font-family:'JetBrains Mono',monospace;font-size:12px;color:var(--texto-secundario);margin-top:8px;margin-bottom:32px;">
        Cláudio Filipe Lima Rapôso
      </p>

      <img
        src="/images/author.jpg"
        alt="Cláudio Rapôso"
        class="sobre-photo"
        loading="lazy"
        width="160"
        height="160"
      />

      <p>
        Sou engenheiro de software especializado em cloud computing, com foco em AWS, Terraform e
        arquitetura de sistemas distribuídos. Trabalho com infraestrutura como código desde quando
        a ideia de "tudo como código" ainda parecia radical — hoje é o padrão.
      </p>

      <p>
        Escrevo em inglês no dev.to e publico as traduções aqui, porque acredito que o conteúdo
        técnico de qualidade precisa existir em português. Não como curiosidade, mas como
        infraestrutura de conhecimento.
      </p>

      <h2>Por que "O Sertão será Cloud"?</h2>

      <p>
        A frase é uma paráfrase de Guimarães Rosa: <em>"O sertão vai virar mar."</em> No Grande
        Sertão: Veredas, o sertão é o lugar do improvável, do que parece longe demais para
        acontecer. Mas acontece.
      </p>

      <p>
        Cloud computing no Brasil ainda carrega esse estigma de coisa de grandes centros, de
        empresas que já chegaram lá. Este blog existe para encurtar essa distância — em português,
        sem jargão desnecessário, com exemplos reais de arquitetura que funcionam aqui.
      </p>

      <p>
        O sertão será cloud. Não como metáfora. Como infraestrutura.
      </p>

      <div class="sobre-social-row" role="list">
        {socials.map(({ href, label, external }) => (
          <a
            href={href}
            class="sobre-social-link"
            role="listitem"
            {...(external ? { target: '_blank', rel: 'noopener noreferrer' } : {})}
          >
            {label}
          </a>
        ))}
      </div>
    </article>
  </div>

  <Footer slot="footer" />
</BaseLayout>
```

The bio paragraphs and thesis text above are complete, publication-ready content — not placeholders. They match the thesis "Por que 'O Sertão será Cloud'" specified in D-14 and REQ-SOBRE-01.
  </action>
  <verify>
    <automated>grep -n "linkedin\|sobre\|Por que" C:/Repo/blog_sertao/src/lib/consts.ts C:/Repo/blog_sertao/src/pages/sobre.astro 2>/dev/null | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/consts.ts` SOCIAL object contains `linkedin: 'https://www.linkedin.com/in/sertaoseracloud'`
    - `src/pages/sobre.astro` exists
    - File imports `{ AUTHOR, SOCIAL }` from `'../lib/consts'`
    - File contains `title="Sobre — Cláudio Rapôso"` in BaseLayout props
    - File contains `<img` with `class="sobre-photo"` and `alt="Cláudio Rapôso"`
    - File contains `h2` with text matching `Por que`
    - File contains `class="sobre-social-row"` with 4 links (GitHub, LinkedIn, X, email)
    - File contains `mailto:engcfraposo@gmail.com`
    - `pnpm build` exits 0 — `/sobre` compiles without TypeScript or Astro errors
    - `pnpm astro check` exits 0
    - `dist/sobre/index.html` exists after build
  </acceptance_criteria>
  <done>consts.ts has linkedin key. /sobre page renders at build time with bio, thesis, and social links. pnpm build succeeds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Plan 01 generated OG images at dist/og/{slug}.png for each post. Plan 02 wired ShareBar into PostLayout. Plan 03 created /sobre. All three plans' build output is now live on the dev server.
  </what-built>
  <how-to-verify>
    Run `pnpm dev` in the project directory, then verify:

    **1. OG images:**
    - Navigate to `http://localhost:4321/og/practical-guide-building-a-cell-based-architecture-on-aws-with-terraform-and-python-n1p.png` (or another post slug)
    - Expected: A 1200×630 dark-navy PNG with the post title in white, cyan separator line, and "Cláudio Rapôso · O Sertão será Cloud" footer
    - Check: title text is readable and wraps correctly at 2 lines if long

    **2. Post og:image meta tags:**
    - View source of any post page at `http://localhost:4321/posts/{slug}`
    - Expected: `<meta property="og:image" content="https://sertaoseracloud.com/og/{slug}.png">` present
    - Expected: `<meta name="twitter:image" content="...">` present

    **3. ShareBar (top of post):**
    - Navigate to `http://localhost:4321/posts/{slug}`
    - Expected: Four buttons appear ABOVE the article header: X, LinkedIn, WhatsApp, Copiar link
    - Click X → opens twitter.com/intent/tweet in new tab with post title and URL in query params
    - Click LinkedIn → opens linkedin.com/sharing/share-offsite in new tab
    - Click WhatsApp → opens wa.me/?text=... in new tab
    - Click Copiar link → label swaps to "Copiado! ✓" for 2 seconds, then reverts to "Copiar link"

    **4. ShareBar (bottom of post):**
    - Scroll to bottom of the same post
    - Expected: Same four buttons appear BELOW the article (above the newsletter section)
    - Click Copiar link on the BOTTOM bar → same label-swap behaviour (verifies no getElementById collision)

    **5. /sobre page:**
    - Navigate to `http://localhost:4321/sobre`
    - Expected: Page renders with heading "Sobre", bio paragraphs, h2 "Por que 'O Sertão será Cloud'?", and social links row (GitHub, LinkedIn, X / Twitter, Email)
    - Author photo: either loads from /images/author.jpg or shows browser broken-image placeholder (both are acceptable — layout must NOT collapse)
    - Social link hover: color transitions to cyan (#00FFFF in dark mode)

    **6. Add author photo (authorial action):**
    - Place your photo at `public/images/author.jpg` (JPG or convert to JPG, 160×160 or larger square crop)
    - Rebuild with `pnpm build` and verify the photo loads on /sobre

    Report any issues with the above checks.
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass. Describe any issues to address before proceeding to REQ-VAL-01 validation.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| consts.ts → sobre.astro | LinkedIn URL is a compile-time constant — no user input, no dynamic URL construction |
| /sobre static page | No form inputs, no user-controlled content, no API calls — pure static page |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-07 | Information Disclosure | mailto:engcfraposo@gmail.com in /sobre | accept | Email address is intentionally public contact information for a public blog. LGPD controller contact is required by /privacidade. No additional mitigation needed. |
| T-08-08 | Tampering | External link rel="noopener noreferrer" | mitigate | All external links (GitHub, LinkedIn, X) have `target="_blank" rel="noopener noreferrer"` applied via the conditional spread in the socials array. mailto: link has no target (correct — opens mail client in same context). |
</threat_model>

<verification>
After all tasks complete (including human checkpoint approval):

1. `pnpm build` exits 0
2. `dist/sobre/index.html` exists
3. `grep "linkedin" src/lib/consts.ts` returns a match with the LinkedIn URL
4. `grep "sobre-social-row" src/pages/sobre.astro` returns a match
5. Human checkpoint passed (OG images visually correct, ShareBar functional on both instances, /sobre page content verified)
</verification>

<success_criteria>
- `src/lib/consts.ts` SOCIAL object has `linkedin` key with a valid URL
- `src/pages/sobre.astro` renders bio narrative, thesis section, and 4 social links
- Author photo `<img class="sobre-photo">` element present (graceful degradation if file absent)
- pnpm build exits 0, dist/sobre/index.html exists
- Human checkpoint: OG images render correctly, ShareBar copy-link works on both top and bottom instances, /sobre content is complete
- REQ-VAL-01: After human approval, validate on LinkedIn Post Inspector (https://www.linkedin.com/post-inspector/) and Twitter Card Validator (https://cards-dev.twitter.com/validator) for a live post URL
</success_criteria>

<output>
After completion, create `.planning/phases/08-share-og-about/08-03-SUMMARY.md` following the summary template at `@C:/Repo/blog_sertao/.claude/get-shit-done/templates/summary.md`.
</output>
