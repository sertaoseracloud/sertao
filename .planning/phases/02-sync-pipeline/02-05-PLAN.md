---
phase: 02-sync-pipeline
plan: 05
type: execute
wave: 4
depends_on: ['02-04']
files_modified:
  - docs/sync-pipeline.md
autonomous: false
requirements:
  - SYNC-10
  - SYNC-11
must_haves:
  truths:
    - "docs/sync-pipeline.md exists with runbook sections covering: common failures (429, timeout, glossary drift, canonical misconfig), how to run manually, how to check logs"
    - "A real dev.to article from @sertaoseracloud is successfully fetched, translated, and produces a draft PR in the GitHub repo"
    - "The draft PR body contains all four D-04 sections: Source, Translation Stats, Glossary Enforcement, Canonical URL Lint"
    - "The translated markdown file exists in src/content/posts/{slug}.md with valid Zod frontmatter"
    - "pnpm build succeeds on the resulting markdown file (Astro validates the frontmatter at build time)"
  artifacts:
    - path: docs/sync-pipeline.md
      provides: "Runbook for the sync pipeline — failure handling, local run instructions, monitoring"
      contains: "## Common Failures"
    - path: src/content/posts/{slug}.md
      provides: "Translated post written by PRBuilder during E2E test run"
      contains: "translated_by: claude-haiku-4-5"
  key_links:
    - from: docs/sync-pipeline.md
      to: ".github/workflows/sync-devto.yml"
      via: "workflow_dispatch manual run instructions"
      pattern: "workflow_dispatch"
    - from: src/content/posts/{slug}.md
      to: src/content.config.ts
      via: "Astro Zod schema validation at pnpm build"
      pattern: "source:"
---

<objective>
Write the sync pipeline runbook and run the end-to-end test: trigger a real sync run against the dev.to account, verify a draft PR is opened with correct frontmatter and PR body, and confirm `pnpm build` accepts the resulting markdown file.

Purpose: SYNC-10 (runbook) and SYNC-11 (E2E gate) are the final acceptance criteria for Phase 2. The runbook documents operational knowledge before it fades; the E2E test proves the full pipeline works in production with a real article.
Output: docs/sync-pipeline.md (committed runbook); draft PR in GitHub (human-visible outcome); pnpm build passing on real translated content.
</objective>

<execution_context>
@C:/Repo/blog_sertao/.claude/get-shit-done/workflows/execute-plan.md
@C:/Repo/blog_sertao/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-sync-pipeline/02-CONTEXT.md
@.planning/phases/02-sync-pipeline/02-RESEARCH.md
@src/content.config.ts
@.planning/glossary.json
@.planning/phases/02-sync-pipeline/02-04-SUMMARY.md

<interfaces>
<!-- Failures the runbook must document — from RESEARCH.md Common Pitfalls -->
<!-- Pitfall A: body_markdown missing from listing response -->
<!-- Pitfall B: reviewer assignment failing silently with GITHUB_TOKEN -->
<!-- Pitfall C: hash collision from whitespace differences -->
<!-- Pitfall D: glossary drift on plurals and hyphenated forms -->
<!-- Pitfall E: multiple PRs for same slug (D-02 intentional behavior) -->
<!-- Pitfall F: local runs write files but no PR (GITHUB_TOKEN required for PRs) -->

<!-- E2E test is a manual workflow_dispatch run — see ROADMAP Phase 2 success criteria -->
<!-- "Teste end-to-end com 1 artigo real do dev.to → PR gerado → markdown traduzido revisável" -->

<!-- Zod schema source of truth for frontmatter validation gate -->
```typescript
// src/content.config.ts
z.object({
  title: z.string().max(80),
  description: z.string().max(200),
  pubDate: z.coerce.date(),
  draft: z.boolean().default(false),
  tags: z.array(z.string()).default([]),
  coverAlt: z.string().optional(),
  source: z.object({
    platform: z.literal('dev.to'),
    id: z.number(),
    url: z.string().url(),
    hash: z.string(),
    synced_at: z.coerce.date(),
    translated_by: z.string(),
  }).optional(),
  canonical_url: z.string().url().optional(),
  manual_override: z.boolean().default(false),
})
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write docs/sync-pipeline.md runbook</name>
  <files>docs/sync-pipeline.md</files>
  <read_first>
    - .planning/phases/02-sync-pipeline/02-RESEARCH.md §"Common Pitfalls" (Pitfalls A through F — must all be covered)
    - .planning/phases/02-sync-pipeline/02-CONTEXT.md §D-02 through D-09 (all decisions that operators need to understand)
    - .github/workflows/sync-devto.yml (exact workflow name and dispatch steps for manual run instructions)
    - .claude/schedules/sync-devto-schedule.md (schedule setup instructions to reference)
    - .claude/triggers/sync-devto-trigger.md (trigger setup instructions to reference)
  </read_first>
  <action>
    Create `docs/` directory and write `docs/sync-pipeline.md` with EXACTLY these sections (minimum content per section specified):

    ```markdown
    # Sync Pipeline Runbook

    **Pipeline:** dev.to → Claude Haiku 4.5 → PT-BR → PR draft
    **Entry point:** `pnpm sync:devto` / `.github/workflows/sync-devto.yml`
    **Last updated:** {date}

    ---

    ## Architecture Overview

    The sync pipeline fetches published articles from `dev.to/@sertaoseracloud` via the Forem API,
    translates each new or changed article from EN to PT-BR using Claude Haiku 4.5, enforces the
    technical glossary, and opens a draft GitHub PR for editorial review.

    ```
    [Claude Code schedule / RemoteTrigger / manual workflow_dispatch]
                    │
                    ▼
    .github/workflows/sync-devto.yml (GH Actions — Ubuntu, Node 22)
                    │
                    ▼
    pnpm sync:devto (scripts/sync-devto.ts)
      │
      ├── DevToClient     — Forem API two-step fetch
      ├── DiffDetector    — SHA-256 change detection
      ├── Translator      — Haiku 4.5 section-by-section
      ├── GlossaryEnforcer — preserve_as_is term validation
      └── PRBuilder       — write .md file + open draft PR via GitHub REST API
    ```

    **Scheduling:**
    - Primary: Claude Code `/schedule` routine (`.claude/schedules/sync-devto-schedule.md`) — fires daily at 03:00 UTC (00:00 BRT)
    - On-demand: Claude Code `RemoteTrigger` (`.claude/triggers/sync-devto-trigger.md`)
    - Manual: GitHub Actions UI → Sync dev.to articles → Run workflow
    - Fallback: Add `schedule: cron: '0 3 * * *'` to `sync-devto.yml` if Claude Code scheduling is unavailable

    ---

    ## Running Manually

    ### Local run (writes files, no PR)
    ```bash
    export ANTHROPIC_API_KEY=sk-ant-...
    pnpm sync:devto
    # Output: markdown files written to src/content/posts/
    # Note: GITHUB_TOKEN not set → no PR created; local preview only
    ```

    ### GitHub Actions manual run (full run with PR)
    1. Go to https://github.com/sertaoseracloud/blog_sertao/actions/workflows/sync-devto.yml
    2. Click **Run workflow** → select branch `main` → click **Run workflow**
    3. Watch the run; PRs appear in https://github.com/sertaoseracloud/blog_sertao/pulls

    ### CLI dispatch (requires GITHUB_TOKEN with `workflow` scope)
    ```bash
    curl -X POST \
      -H "Authorization: Bearer $GITHUB_TOKEN" \
      -H "Accept: application/vnd.github+json" \
      -H "X-GitHub-Api-Version: 2022-11-28" \
      https://api.github.com/repos/sertaoseracloud/blog_sertao/actions/workflows/sync-devto.yml/dispatches \
      -d '{"ref":"main"}'
    # 204 No Content = dispatch accepted
    ```

    ---

    ## Environment Variables

    | Variable | Where set | Required | Notes |
    |----------|-----------|----------|-------|
    | `ANTHROPIC_API_KEY` | GitHub Actions secret | Yes | Create in Anthropic Console; add to repo secrets |
    | `GITHUB_TOKEN` | Auto-injected by GH Actions | Yes (for PRs) | Not needed for local file-only runs |
    | `MAX_TRANSLATIONS_PER_RUN` | Env var (default: 5) | No | Circuit breaker D-08; override via workflow_dispatch input |

    ---

    ## Common Failures

    ### 429 Rate Limit (Anthropic API)
    **Symptom:** Run fails with `RateLimitError` in logs after retries exhausted.
    **Cause:** Too many Haiku API calls in a short period. The SDK retries 3× with exponential backoff automatically; this error only fires after all retries fail.
    **Fix:** The failed article will be retried on the next cron run (next day). No manual action needed unless this happens consistently.
    **Prevention:** `MAX_TRANSLATIONS_PER_RUN=5` limits calls per run; Anthropic $5/month budget alert is the secondary gate.

    ### Timeout / Network Error
    **Symptom:** `APIConnectionError` in logs; run exits 1.
    **Cause:** Transient network failure between GH Actions runner and Anthropic/dev.to API.
    **Fix:** Re-run the workflow manually (GitHub Actions UI → Re-run failed jobs) or wait for the next cron run.

    ### Glossary Drift — No PR Created
    **Symptom:** Log line `[sync] GLOSSARY DRIFT for id=XXXX. Issue opened. Skipping PR.`; a GitHub Issue is opened automatically.
    **Cause:** One or more `preserve_as_is` terms from `.planning/glossary.json` appear fewer times in the PT translation than in the EN source (per D-09 — hard fail on any drift).
    **Fix:**
    1. Read the GitHub Issue — it lists the drifted terms with EN/PT counts.
    2. Option A (preferred): Add the plural/variant form to `preserve_as_is` in `.planning/glossary.json` (e.g., add `"Lambdas"` if `"Lambda"` triggers false positive).
    3. Option B: Accept the drift for this article — close the Issue and add the article slug to a skip list (not currently implemented; v2).
    4. Re-run the sync after updating the glossary.

    ### Canonical URL Missing — Issue Opened, PR Still Created
    **Symptom:** Log line `[sync] canonical_url missing/incorrect for id=XXXX. Issue opened (non-blocking).`; PR is still created (D-06: non-blocking).
    **Cause:** The dev.to article does not have `canonical_url` pointing to `https://sertaoseracloud.com/posts/`.
    **Fix:**
    1. Read the GitHub Issue — it has the exact canonical URL value to set.
    2. Go to the dev.to article → Edit → More options → Canonical URL → paste the value.
    3. The next sync run will detect no change (same `body_markdown` hash) so no new PR will be opened; the canonical URL will be correct if you set it before merging the current PR.

    ### Multiple Open PRs for Same Article (D-02 Behavior)
    **Symptom:** Two or more PRs open for the same article slug.
    **Cause:** Expected behavior per D-02. When an article changes, a new PR is always created. The old PR is not automatically closed.
    **Fix:** Close the older PR(s) manually. The PR title includes `dev.to #{id}` for easy identification and filtering.

    ### Local Run Writes Files but No PR
    **Symptom:** `pnpm sync:devto` writes markdown to `src/content/posts/` but no PR appears in GitHub.
    **Cause:** Expected behavior. PRs are only created when `GITHUB_TOKEN` is set. Local runs intentionally skip PR creation (useful for reviewing translation quality before committing).
    **Fix:** This is not a bug. To open a PR, run via GitHub Actions (see "Running Manually" above).

    ### `body_markdown` Undefined — Hash Constant Across All Articles
    **Symptom:** ALL articles appear "changed" every run; circuit breaker fires on every run.
    **Cause:** DevToClient is incorrectly reading `body_markdown` from the listing response (which does not include it). This would be a regression bug in the two-step fetch.
    **Fix:** Check `scripts/devto-client.ts` — `getArticle(id)` must be called for each article. The listing response (`listArticles`) intentionally does NOT return `body_markdown`.

    ---

    ## Secrets Setup (First-time)

    ### 1. ANTHROPIC_API_KEY
    1. Log in to https://console.anthropic.com
    2. Go to **API Keys** → **Create Key** → copy the value
    3. Go to https://github.com/sertaoseracloud/blog_sertao/settings/secrets/actions
    4. Click **New repository secret** → name: `ANTHROPIC_API_KEY` → paste value → **Add secret**

    ### 2. Budget Alert (Anthropic)
    1. In Anthropic Console → **Billing** → **Usage limits**
    2. Set **Monthly spend limit** to `$5`
    3. This prevents runaway API cost if the DiffDetector has a bug

    ### 3. GH_PAT (Optional — only if reviewer assignment fails)
    If PRs are created but `sertaoseracloud` is not assigned as reviewer:
    1. Go to https://github.com/settings/personal-access-tokens/new
    2. Create a Fine-grained PAT scoped to `sertaoseracloud/blog_sertao` with `Contents: read/write` + `Pull requests: read/write`
    3. Add as repo secret named `GH_PAT`
    4. Update `sync-devto.yml` line: `GITHUB_TOKEN: ${{ secrets.GH_PAT }}` instead of `secrets.GITHUB_TOKEN`

    ---

    ## Monitoring

    | Signal | Where to check |
    |--------|---------------|
    | Daily sync ran | https://github.com/sertaoseracloud/blog_sertao/actions/workflows/sync-devto.yml |
    | New translation PRs | https://github.com/sertaoseracloud/blog_sertao/pulls?q=is:pr+label:sync |
    | Glossary drift issues | https://github.com/sertaoseracloud/blog_sertao/issues?q=is:issue+label:glossary-drift |
    | Canonical URL issues | https://github.com/sertaoseracloud/blog_sertao/issues?q=is:issue+%5Bcanonical%5D |
    | Anthropic spend | https://console.anthropic.com/billing |

    ---

    ## Updating the Glossary

    `.planning/glossary.json` controls both the Translator (system prompt) and the GlossaryEnforcer (drift detection).

    **When to update:**
    - After merging a PR where you manually corrected a translated term → add the variant to `preserve_as_is` or `prefer_en_over_pt`
    - When a new cloud service becomes relevant → add its name to `preserve_as_is`
    - When GlossaryEnforcer fires a false positive on a plural/variant → add the variant form

    **Bump `version`** in `glossary.json` after each update so the next run's PR body logs the new version.

    ---

    ## Pipeline Decisions Reference

    | Decision | Summary |
    |----------|---------|
    | D-01 | Section-by-section translation (split on H2/H3) — one Haiku call per section |
    | D-02 | Re-sync always creates a new PR draft; old PRs closed manually |
    | D-03 | `manual_override: true` in frontmatter → article skipped entirely |
    | D-04 | PR body must include: Source, Translation Stats, Glossary Enforcement, Canonical URL Lint |
    | D-05 | PR is draft; `sertaoseracloud` assigned as reviewer |
    | D-06 | Missing canonical_url → opens GitHub Issue (non-blocking); PR still created |
    | D-07 | Canonical check: starts with `https://sertaoseracloud.com/posts/` (prefix only) |
    | D-08 | `MAX_TRANSLATIONS_PER_RUN=5` circuit breaker per run |
    | D-09 | Glossary drift → hard fail (no PR for that article), opens GitHub Issue, other articles continue |
    ```
  </action>
  <verify>
    <automated>test -f docs/sync-pipeline.md && grep -q "## Common Failures" docs/sync-pipeline.md && grep -q "429" docs/sync-pipeline.md && grep -q "Glossary Drift" docs/sync-pipeline.md && grep -q "canonical" docs/sync-pipeline.md && grep -q "ANTHROPIC_API_KEY" docs/sync-pipeline.md && grep -q "MAX_TRANSLATIONS_PER_RUN" docs/sync-pipeline.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/sync-pipeline.md` exists
    - Contains `## Common Failures` section
    - Contains `429` (rate limit failure)
    - Contains `Timeout` or network error failure
    - Contains `Glossary Drift` failure with fix instructions
    - Contains `Canonical URL` failure with fix instructions
    - Contains `Multiple Open PRs` behavior explanation (D-02)
    - Contains `ANTHROPIC_API_KEY` secret setup steps
    - Contains budget alert setup instructions
    - Contains manual run instructions (GH Actions UI + CLI curl)
    - Contains `## Pipeline Decisions Reference` table (all D-01 through D-09)
    - Contains monitoring table with links
  </acceptance_criteria>
  <done>Runbook written covering all common failure modes, secret setup, manual run options, and pipeline decisions reference.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: End-to-end test — real dev.to article → draft PR (SYNC-11)</name>
  <read_first>
    - .github/workflows/sync-devto.yml (verify workflow_dispatch is the trigger before running)
    - docs/sync-pipeline.md §"Running Manually" (exact steps to follow)
    - .planning/phases/02-sync-pipeline/02-CONTEXT.md §D-04, D-05 (PR body and reviewer requirements to verify)
  </read_first>
  <what-built>
    Plans 02-01 through 02-04 have implemented the full sync pipeline:
    - DevToClient (two-step Forem API fetch)
    - DiffDetector (SHA-256 change detection)
    - Translator (Haiku 4.5 section-by-section with glossary)
    - GlossaryEnforcer (preserve_as_is drift detection)
    - PRBuilder (markdown file writer + GitHub REST API PR creation)
    - sync-devto.ts (orchestrator with circuit breaker + manual_override skip)
    - sync-devto.yml (workflow_dispatch-only GH Actions workflow)
    All unit tests pass (`pnpm test:sync` exits 0).
  </what-built>
  <how-to-verify>
    **Pre-conditions to check before running:**
    1. Confirm `ANTHROPIC_API_KEY` is set as a GitHub Actions secret (Settings → Secrets and variables → Actions → must see `ANTHROPIC_API_KEY` in the list)
    2. Confirm at least one article is published on dev.to at https://dev.to/sertaoseracloud (any public article works)
    3. Optional (recommended): Set `canonical_url` on one dev.to article to `https://sertaoseracloud.com/posts/{slug}` before running — this tests the canonical PASS path

    **Run the E2E test:**
    1. Go to https://github.com/sertaoseracloud/blog_sertao/actions/workflows/sync-devto.yml
    2. Click **Run workflow** → branch: `main` → `max_translations`: leave default (5) → click **Run workflow**
    3. Watch the job logs (click the running job) — look for:
       - `[sync-devto] Found N articles for @sertaoseracloud`
       - `[sync] ` log lines showing which articles are processed/skipped
       - `[PRBuilder] Draft PR opened: https://github.com/...`
    4. Wait for the job to complete (should be <3 minutes for 1-2 articles)

    **Verify the PR:**
    1. Go to https://github.com/sertaoseracloud/blog_sertao/pulls
    2. Find the PR titled `[sync] {article title} (dev.to #{id})`
    3. Verify the PR is a **draft** (grey "Draft" badge, not green "Open")
    4. Verify `sertaoseracloud` is listed as **Reviewer** in the right sidebar
    5. Open the PR and verify the body contains all four sections:
       - `## Source` (with article title, dev.to URL, synced_at timestamp)
       - `## Translation Stats` (sections count, model `claude-haiku-4-5`, token estimate)
       - `## Glossary Enforcement` (PASS ✓ or WARN ⚠ with term list)
       - `## Canonical URL Lint` (✓ or ⚠)
    6. Click **Files changed** tab — verify `src/content/posts/{slug}.md` was added
    7. Inspect the file — verify frontmatter includes:
       - `source.platform: dev.to`
       - `source.translated_by: claude-haiku-4-5`
       - `source.hash:` (64-char hex string)
       - `manual_override: false`

    **Verify the markdown builds:**
    1. Merge the PR (or cherry-pick the branch locally)
    2. Run `pnpm build` — must exit 0 with no Zod schema errors
    3. Alternatively: run `pnpm astro check` on the new `.md` file

    **What to report back:**
    - PR URL (e.g., `https://github.com/sertaoseracloud/blog_sertao/pull/N`)
    - Whether the PR body has all four sections (yes/no per section)
    - Whether `sertaoseracloud` is assigned as reviewer (yes/no)
    - Whether `pnpm build` passes after the file is present (yes/no)
    - Any errors seen in GH Actions logs
  </how-to-verify>
  <resume-signal>
    Type "approved" if:
    - A draft PR was opened
    - PR body has all four D-04 sections
    - src/content/posts/{slug}.md is present in the PR diff
    - pnpm build (or astro check) passes on the translated file

    Or describe any issues found so they can be diagnosed and fixed before closing Phase 2.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Translated markdown → Astro build | PRBuilder-written file is consumed by Astro build; Zod schema is the validation gate |
| E2E test → real Anthropic API | First production call with real ANTHROPIC_API_KEY; costs real money (~$0.06 for 1 article) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-23 | Information Disclosure | docs/sync-pipeline.md | accept | Runbook documents architecture but contains no secrets; GH repo is public; no sensitive info in runbook |
| T-02-24 | Denial of Service (cost) | E2E test run | mitigate | max_translations=5 circuit breaker; $5 budget alert already configured (SYNC-09); first real run expected to translate 1-2 articles at ~$0.06 each |
| T-02-25 | Tampering | src/content/posts/{slug}.md from E2E | mitigate | Astro Zod schema validates frontmatter at pnpm build time; malformed frontmatter causes build failure (not silent corruption) |
</threat_model>

<verification>
- `test -f docs/sync-pipeline.md` — runbook exists
- `grep -q "## Common Failures" docs/sync-pipeline.md` — failures section present
- `grep -q "D-08" docs/sync-pipeline.md` — circuit breaker documented
- `grep -q "D-09" docs/sync-pipeline.md` — glossary drift documented
- Human gate: PR opened, all four D-04 sections present, `pnpm build` passes
</verification>

<success_criteria>
- SYNC-10: docs/sync-pipeline.md runbook committed with all failure modes, secret setup, manual run instructions, and decisions reference
- SYNC-11: End-to-end test passed — at least 1 real dev.to article translated and a draft PR opened, reviewed and confirmed by author, `pnpm build` succeeds on translated content
- Phase 2 complete: all SYNC-01 through SYNC-11 requirements satisfied across plans 02-01 through 02-05
</success_criteria>

<output>
After completion, create `.planning/phases/02-sync-pipeline/02-05-SUMMARY.md` documenting:
- URL of the E2E test PR
- Article title and dev.to ID that was translated
- Whether reviewer assignment worked with GITHUB_TOKEN (or PAT fallback was needed)
- Any glossary drift detected on first run (and terms added to glossary.json if fixed)
- pnpm build result on the translated post
- Confirmation Phase 2 is complete (SYNC-01 through SYNC-11 all satisfied)
</output>
