---
phase: 01-bootstrap-fundacoes
plan: 02
type: execute
wave: 2
depends_on: ['01-01']
files_modified:
  - src/styles/global.css
  - src/layouts/BaseLayout.astro
  - src/components/Header.astro
  - src/components/Footer.astro
  - src/pages/index.astro
  - .planning/ROADMAP.md
autonomous: true
requirements_addressed:
  - REQ-1.1
  - REQ-1.2
  - REQ-1.4
requirements:
  - REQ-1.1
  - REQ-1.2
  - REQ-1.4
user_setup: []
must_haves:
  truths:
    - "src/styles/global.css exists with :root design tokens"
    - "BaseLayout.astro imports global.css and sets lang='pt-BR'"
    - "Header.astro renders .site-nav with brand name"
    - "Footer.astro renders .footer with sig element"
    - "pnpm build succeeds without errors"
  artifacts:
    - path: src/styles/global.css
      provides: "Código Chama Azul design tokens, fonts, base styles, component CSS"
      contains: "--nucleo-eletrico"
    - path: src/layouts/BaseLayout.astro
      provides: "HTML shell with lang=pt-BR, font preload, theme init, ambient bg"
      contains: "lang=\"pt-BR\""
    - path: src/components/Header.astro
      provides: "Site navigation using .site-nav design system class"
      contains: "site-nav"
    - path: src/components/Footer.astro
      provides: "Footer using .footer design system class"
      contains: "footer"
  key_links:
    - from: BaseLayout.astro
      to: src/styles/global.css
      via: "import statement"
      pattern: "global.css"
    - from: index.astro
      to: BaseLayout.astro
      via: "layout import"
      pattern: "BaseLayout"
---

<objective>
Integrate the "Código Chama Azul" design system (extracted from blog-design-system/Blog Design System.html) into the Astro project as functional layout components. The CSS tokens are already in src/styles/global.css; this plan wires them into Astro layouts and creates the site chrome components (Header, Footer, BaseLayout).

Design system source: blog-design-system/Blog Design System.html
Section coverage: Site Nav (01), Reading View (05), Components (06)
Token file: src/styles/global.css (already created)

Also updates ROADMAP.md Phase 1 scope to formally include the design system.
</objective>

<execution_context>
@C:/Repo/blog_sertao/.claude/get-shit-done/workflows/execute-plan.md
@C:/Repo/blog_sertao/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-bootstrap-fundacoes/01-CONTEXT.md
@.planning/research/ARCHITECTURE.md
@src/styles/global.css

<interfaces>
<!-- BaseLayout.astro exports -->
BaseLayout accepts props:
- title: string
- description?: string
- image?: string (OG placeholder for Phase 3)
- pubDate?: Date (for post layouts)

<!-- Header.astro -->
Header uses SITE_TITLE and SITE_URL from src/lib/consts.ts (created in 01-01)
Nav links: Início (/) · Posts (/posts) · Sobre (/sobre) (static, polished in Phase 8)

<!-- Footer.astro -->
Footer uses AUTHOR and SOCIAL from src/lib/consts.ts
Sig: "Feito com ☁ por <b>sertaoseracloud</b>"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Update ROADMAP.md Phase 1 scope to include design system</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (current Phase 1 scope block)
    - blog-design-system/Blog Design System.html (title confirms: "Sistema de Design · Blog · Código Chama Azul")
  </read_first>
  <action>
    Edit `.planning/ROADMAP.md` Phase 1 "Scope in:" block to add two lines after "Design tokens brand-aware semânticos":
    ```
    - Design system "Código Chama Azul" (blog-design-system/Blog Design System.html) — tokens, fonts, component CSS
    - Astro layout components: BaseLayout, Header, Footer using design system tokens
    ```

    Also add a note to the "Threat model" block:
    ```
    - Design system supersedes Sertão brand colors from early planning — see blog-design-system/ for source of truth
    ```

    Do NOT change phase goal, success criteria, or other phases.
  </action>
  <verify>
    <automated>grep -q "Código Chama Azul" .planning/ROADMAP.md && grep -q "BaseLayout, Header, Footer" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/ROADMAP.md` contains "Código Chama Azul"
    - `.planning/ROADMAP.md` contains "BaseLayout, Header, Footer"
  </acceptance_criteria>
  <done>ROADMAP.md Phase 1 updated to document design system.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create BaseLayout.astro</name>
  <files>src/layouts/BaseLayout.astro</files>
  <read_first>
    - src/styles/global.css (all tokens and .ambient class)
    - src/lib/consts.ts (SITE_TITLE, SITE_URL — created by 01-01; if not yet, use literals)
    - .planning/phases/01-bootstrap-fundacoes/01-CONTEXT.md §D-11, D-13 (font preload strategy)
    - .planning/research/ARCHITECTURE.md §"Component Responsibilities" (BaseLayout role)
  </read_first>
  <action>
    Create `src/layouts/BaseLayout.astro` with this EXACT content:

    ```astro
    ---
    import '../styles/global.css';

    interface Props {
      title: string;
      description?: string;
      image?: string;
      pubDate?: Date;
    }

    const {
      title,
      description = 'Blog sobre cloud computing em português — O Sertão será Cloud.',
      image,
      pubDate,
    } = Astro.props;

    const SITE_TITLE = 'O Sertão será Cloud';
    const SITE_URL = 'https://sertaoseracloud.com';
    const canonicalURL = new URL(Astro.url.pathname, SITE_URL);
    ---

    <!doctype html>
    <html lang="pt-BR">
      <head>
        <meta charset="utf-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1" />
        <meta name="generator" content={Astro.generator} />
        <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
        <link rel="canonical" href={canonicalURL} />

        <!-- Primary meta -->
        <title>{title} · {SITE_TITLE}</title>
        <meta name="description" content={description} />

        <!-- Open Graph (Phase 3 will enrich) -->
        <meta property="og:type" content="website" />
        <meta property="og:url" content={canonicalURL} />
        <meta property="og:title" content={`${title} · ${SITE_TITLE}`} />
        <meta property="og:description" content={description} />
        <meta property="og:locale" content="pt_BR" />
        {image && <meta property="og:image" content={new URL(image, SITE_URL)} />}

        <!-- Theme init: prevent FOUC by reading localStorage before paint -->
        <script is:inline>
          (function () {
            const stored = localStorage.getItem('theme');
            if (stored) document.documentElement.setAttribute('data-theme', stored);
          })();
        </script>
      </head>
      <body>
        <!-- Ambient grid + gradient backdrop -->
        <div class="ambient" aria-hidden="true"></div>

        <!-- Site chrome -->
        <slot name="header" />

        <!-- Page content -->
        <main id="main-content">
          <slot />
        </main>

        <!-- Site footer -->
        <slot name="footer" />
      </body>
    </html>
    ```

    Notes:
    - `import '../styles/global.css'` wires all design tokens and component classes
    - `lang="pt-BR"` satisfies D-09 / Pitfall 9
    - `.ambient` renders the radial-gradient + grid overlay from the design system
    - `data-theme` attribute hook satisfies D-05 (Phase 4 will populate dark values)
    - Slots for header/footer so they can be placed by page layouts
    - OG/meta stubs satisfy Phase 3 extension points
  </action>
  <verify>
    <automated>test -f src/layouts/BaseLayout.astro && grep -q 'lang="pt-BR"' src/layouts/BaseLayout.astro && grep -q "global.css" src/layouts/BaseLayout.astro && grep -q "ambient" src/layouts/BaseLayout.astro && grep -q "data-theme" src/layouts/BaseLayout.astro</automated>
  </verify>
  <acceptance_criteria>
    - `src/layouts/BaseLayout.astro` exists
    - Contains `lang="pt-BR"`
    - Contains `import '../styles/global.css'`
    - Contains `class="ambient"`
    - Contains `data-theme` in the inline script
    - Contains `<slot />` for main content
  </acceptance_criteria>
  <done>BaseLayout.astro created with design system import and lang="pt-BR".</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create Header.astro</name>
  <files>src/components/Header.astro</files>
  <read_first>
    - src/styles/global.css §"07 · Site Nav" (all .site-nav, .site-brand, .nav-links classes)
    - .planning/phases/01-bootstrap-fundacoes/01-CONTEXT.md §D-20 (SITE_TITLE, AUTHOR values)
  </read_first>
  <action>
    Create `src/components/Header.astro`:

    ```astro
    ---
    const navLinks = [
      { href: '/', label: 'Início' },
      { href: '/posts', label: 'Posts' },
      { href: '/sobre', label: 'Sobre' },
    ];

    const currentPath = Astro.url.pathname;
    ---

    <header>
      <nav class="site-nav" aria-label="Navegação principal">
        <!-- Brand -->
        <a href="/" class="site-brand" aria-label="O Sertão será Cloud — início">
          <span class="logo-mark" aria-hidden="true">
            <svg viewBox="0 0 18 18" fill="none" xmlns="http://www.w3.org/2000/svg">
              <path d="M9 2l1.8 5.4H16l-4.3 3.1 1.6 5L9 12.6l-4.3 3-1.6-5L4.3 7.5 9 4.3V2z" fill="currentColor"/>
            </svg>
          </span>
          O <b>Sertão</b><span class="tld">.cloud</span>
        </a>

        <!-- Nav links -->
        <nav class="nav-links" aria-label="Links de navegação">
          {navLinks.map(({ href, label }) => (
            <a
              href={href}
              aria-current={currentPath === href || (href !== '/' && currentPath.startsWith(href)) ? 'page' : undefined}
            >
              {label}
            </a>
          ))}
        </nav>

        <!-- Actions placeholder (Phase 4: dark mode toggle, Phase 7: search) -->
        <div class="nav-actions" aria-label="Ações">
          <span class="text-muted" style="font-family:'JetBrains Mono',monospace;font-size:11px;letter-spacing:0.1em;">
            ☁ PT-BR
          </span>
        </div>
      </nav>
    </header>
    ```

    The `.site-nav`, `.site-brand`, `.logo-mark`, `.nav-links` classes are all defined in `src/styles/global.css`. No additional CSS needed here.
  </action>
  <verify>
    <automated>test -f src/components/Header.astro && grep -q "site-nav" src/components/Header.astro && grep -q "site-brand" src/components/Header.astro && grep -q "nav-links" src/components/Header.astro</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/Header.astro` exists
    - Contains `class="site-nav"`
    - Contains `class="site-brand"`
    - Contains `class="nav-links"`
    - Contains `aria-label` attributes (accessibility)
    - Nav links array includes Início, Posts, Sobre
  </acceptance_criteria>
  <done>Header.astro created using .site-nav design system class.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Create Footer.astro</name>
  <files>src/components/Footer.astro</files>
  <read_first>
    - src/styles/global.css §"13 · Footer" (.footer, .sig classes)
    - .planning/phases/01-bootstrap-fundacoes/01-CONTEXT.md §D-20 (AUTHOR, SOCIAL values)
  </read_first>
  <action>
    Create `src/components/Footer.astro`:

    ```astro
    ---
    const currentYear = new Date().getFullYear();

    const socials = [
      { href: 'https://dev.to/sertaoseracloud', label: 'dev.to' },
      { href: 'https://github.com/sertaoseracloud', label: 'GitHub' },
      { href: '/rss.xml', label: 'RSS' },
    ];
    ---

    <footer class="footer stage">
      <div style="display:flex; justify-content:space-between; align-items:center; flex-wrap:wrap; gap:16px;">
        <p class="sig">
          Feito com ☁ por <b>sertaoseracloud</b> · {currentYear}
        </p>

        <nav aria-label="Links sociais" style="display:flex;gap:20px;">
          {socials.map(({ href, label }) => (
            <a
              href={href}
              class="text-muted"
              style="font-family:'Chakra Petch',sans-serif;font-size:11px;letter-spacing:0.14em;text-transform:uppercase;text-decoration:none;transition:color 0.2s;"
              {...(href.startsWith('http') ? { target: '_blank', rel: 'noopener noreferrer' } : {})}
            >
              {label}
            </a>
          ))}
        </nav>
      </div>
    </footer>
    ```
  </action>
  <verify>
    <automated>test -f src/components/Footer.astro && grep -q "class=\"footer" src/components/Footer.astro && grep -q "sig" src/components/Footer.astro && grep -q "sertaoseracloud" src/components/Footer.astro</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/Footer.astro` exists
    - Contains `class="footer`
    - Contains `.sig` element with "sertaoseracloud"
    - Contains social links (dev.to, GitHub, RSS)
    - RSS link points to `/rss.xml` (Phase 3 will generate the file)
  </acceptance_criteria>
  <done>Footer.astro created using .footer and .sig design system classes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Create index.astro homepage</name>
  <files>src/pages/index.astro</files>
  <read_first>
    - src/layouts/BaseLayout.astro (slot names: header, footer, default)
    - src/components/Header.astro
    - src/components/Footer.astro
    - src/styles/global.css (available classes: .stage, .cards, .tag-row, .tag)
    - .planning/phases/01-bootstrap-fundacoes/01-CONTEXT.md §D-15 (mock post "Hello, Sertão!")
  </read_first>
  <action>
    Create `src/pages/index.astro`:

    ```astro
    ---
    import BaseLayout from '../layouts/BaseLayout.astro';
    import Header from '../components/Header.astro';
    import Footer from '../components/Footer.astro';
    import { getCollection } from 'astro:content';

    const allPosts = await getCollection('posts', ({ data }) =>
      import.meta.env.PROD ? !data.draft : true,
    );

    const recentPosts = allPosts
      .sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf())
      .slice(0, 6);
    ---

    <BaseLayout title="Início" description="Blog sobre cloud computing em português — O Sertão será Cloud.">
      <Header slot="header" />

      <div class="stage">
        <!-- Hero -->
        <section style="padding: 64px 0 48px; max-width: 720px;">
          <p style="font-family:'Chakra Petch',sans-serif;font-size:11px;letter-spacing:0.28em;text-transform:uppercase;color:var(--nucleo-eletrico);margin-bottom:16px;">
            ☁ Cloud Computing · PT-BR
          </p>
          <h1 style="font-size:clamp(44px,4.2vw,68px);font-weight:700;line-height:0.96;letter-spacing:-0.015em;color:#E9FBFF;margin:0 0 24px;">
            O Sertão<br/><em style="color:var(--nucleo-eletrico);font-style:italic;">será Cloud</em>
          </h1>
          <p style="font-size:18px;color:var(--texto-secundario);max-width:58ch;font-weight:300;line-height:1.6;">
            Tutoriais, análises e opinião sobre cloud computing — direto do Sertão pra nuvem.
          </p>
        </section>

        <!-- Recent posts grid -->
        {recentPosts.length > 0 ? (
          <section>
            <p style="font-family:'Chakra Petch',sans-serif;font-size:11px;letter-spacing:0.2em;text-transform:uppercase;color:var(--texto-secundario);margin-bottom:24px;">
              Posts recentes
            </p>
            <div class="cards">
              {recentPosts.map((post) => (
                <a href={`/posts/${post.slug}`} class="card" style="text-decoration:none;">
                  <div class={`card-media p-${post.data.tags?.[0] ?? 'arq'}`}></div>
                  <div class="card-body">
                    <div class="tag-row" style="margin-bottom:8px;">
                      {post.data.tags?.slice(0,2).map((tag: string) => (
                        <span class="tag"><span class="hash">#</span>{tag}</span>
                      ))}
                    </div>
                    <h3>{post.data.title}</h3>
                    <p class="card-meta">
                      {new Date(post.data.pubDate).toLocaleDateString('pt-BR', { dateStyle: 'medium' })}
                    </p>
                  </div>
                </a>
              ))}
            </div>
          </section>
        ) : (
          <section style="padding:48px 0;text-align:center;border:1px dashed var(--hairline);">
            <p style="color:var(--texto-secundario);font-family:'JetBrains Mono',monospace;font-size:13px;">
              // posts chegam via pipeline dev.to → Phase 2
            </p>
          </section>
        )}
      </div>

      <Footer slot="footer" />
    </BaseLayout>
    ```

    Notes:
    - Uses `getCollection` — requires Phase 01-01 to have run `pnpm install` first
    - Empty state shown when no posts exist yet (pre-Phase 2)
    - `.stage` class limits width to 1520px and adds standard padding
    - Card gradient class (`p-arq`, `p-cloud`, etc.) derived from first tag
  </action>
  <verify>
    <automated>test -f src/pages/index.astro && grep -q "BaseLayout" src/pages/index.astro && grep -q "getCollection" src/pages/index.astro && grep -q "Header" src/pages/index.astro && grep -q "Footer" src/pages/index.astro</automated>
  </verify>
  <acceptance_criteria>
    - `src/pages/index.astro` exists
    - Imports BaseLayout, Header, Footer
    - Uses `getCollection('posts', ...)` with PROD draft filter
    - Contains hero section with "O Sertão será Cloud" text
    - Renders `.cards` grid for recent posts
    - Falls back to empty-state message when no posts exist
  </acceptance_criteria>
  <done>Homepage created with hero, card grid, and empty-state for pre-Phase-2.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 6: Add favicon.svg placeholder</name>
  <files>public/favicon.svg</files>
  <read_first>
    - .planning/phases/01-bootstrap-fundacoes/01-CONTEXT.md §"Claude's Discretion" (favicon placeholder instructions)
    - src/styles/global.css (--nucleo-eletrico: #00FFFF, --abismo-profundo: #0A0F1E)
  </read_first>
  <action>
    Create `public/favicon.svg` — a minimal star/flame symbol matching the design system (dark bg, cyan accent):

    ```svg
    <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 32 32">
      <rect width="32" height="32" rx="6" fill="#0A0F1E"/>
      <path d="M16 6l2.4 7.2H26l-6.2 4.5L22 25l-6-4.4L10 25l2.2-7.3L6 13.2h7.6z" fill="#00FFFF"/>
    </svg>
    ```

    This is the star/flame motif from the design file thumbnail. Phase 8 will replace with polished brand mark.
  </action>
  <verify>
    <automated>test -f public/favicon.svg && grep -q "#00FFFF" public/favicon.svg && grep -q "#0A0F1E" public/favicon.svg</automated>
  </verify>
  <acceptance_criteria>
    - `public/favicon.svg` exists
    - Contains `#00FFFF` (cyan — nucleo-eletrico)
    - Contains `#0A0F1E` (dark bg — abismo-profundo)
    - Is valid SVG (has `<svg` and `</svg>` tags)
  </acceptance_criteria>
  <done>Favicon placeholder created using design system colors.</done>
</task>

</tasks>

<verification>
  - `src/styles/global.css` exists and contains `--nucleo-eletrico`
  - `src/layouts/BaseLayout.astro` imports `global.css` and sets `lang="pt-BR"`
  - `src/components/Header.astro` contains `.site-nav` class
  - `src/components/Footer.astro` contains `.footer` class
  - `src/pages/index.astro` renders BaseLayout with Header and Footer slots
  - `public/favicon.svg` exists with design system colors
  - `.planning/ROADMAP.md` mentions "Código Chama Azul"
  - After Plan 01-01 installs deps: `pnpm build` emits `dist/` without errors
</verification>

<success_criteria>
  - REQ-1.1 (`pnpm dev` roda localmente): layout + pages in place; smoke test after 01-01 installs deps
  - REQ-1.2 (`pnpm build` produz dist/ sem warnings): all Astro components valid
  - REQ-1.4 (paleta semântica em CSS vars): `--nucleo-eletrico`, `--abismo-profundo`, `--chama-primaria` etc. all defined in global.css and consumed by components
  - Design system "Código Chama Azul" formally integrated into Phase 1 scope
</success_criteria>

<output>
After completion, create `.planning/phases/01-bootstrap-fundacoes/01-02-SUMMARY.md` documenting:
- Which Astro components were created
- Design system token count (from global.css)
- Any deviations from 01-CONTEXT.md decisions (notably: fonts changed from Inter/Fira Code to Space Grotesk/Chakra Petch/JetBrains Mono per design file)
- Confirmation that `pnpm build` succeeded (or note that it awaits Plan 01-01 execution)
</output>
