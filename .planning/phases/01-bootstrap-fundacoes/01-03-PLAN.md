---
phase: 01-bootstrap-fundacoes
plan: 03
type: execute
wave: 3
depends_on: [01-01, 01-02]
files_modified:
  - .github/workflows/deploy.yml
  - public/CNAME
  - src/styles/global.css
autonomous: true
gap_closure: true
requirements_addressed:
  - REQ-1.3
  - REQ-1.4
  - REQ-1.7
requirements:
  - REQ-1.3
  - REQ-1.4
  - REQ-1.7
user_setup:
  - service: github-pages
    why: "GitHub Pages must be enabled with GitHub Actions as the source before the deploy workflow can publish."
    dashboard_config:
      - task: "Enable GitHub Pages via GitHub Actions"
        location: "GitHub repo → Settings → Pages → Build and deployment → Source: GitHub Actions"
      - task: "Verify CNAME file is recognised (custom domain field auto-populated after first deploy)"
        location: "GitHub repo → Settings → Pages → Custom domain — should show sertaoseracloud.com after first successful workflow run"
  - service: dns-registrar
    why: "Apex domain A records must point to GitHub Pages IPs for HTTPS to work."
    dashboard_config:
      - task: "Add 4 A records for sertaoseracloud.com pointing to GitHub Pages IPs"
        location: "DNS registrar control panel for sertaoseracloud.com"
        values: |
          A  @  185.199.108.153
          A  @  185.199.109.153
          A  @  185.199.110.153
          A  @  185.199.111.153
      - task: "(Optional) Add AAAA records for IPv6"
        location: "DNS registrar control panel"
        values: |
          AAAA  @  2606:50c0:8000::153
          AAAA  @  2606:50c0:8001::153
          AAAA  @  2606:50c0:8002::153
          AAAA  @  2606:50c0:8003::153

must_haves:
  truths:
    - "Pushing to main triggers a GitHub Actions workflow that builds and deploys the Astro site"
    - "sertaoseracloud.com is the canonical domain used by GitHub Pages (CNAME file present)"
    - "ROADMAP Phase 1 CSS token success criterion is satisfied by the implemented design system"
  artifacts:
    - path: ".github/workflows/deploy.yml"
      provides: "GitHub Actions workflow that builds with pnpm and deploys to GitHub Pages"
      contains: "actions/deploy-pages@v4"
    - path: "public/CNAME"
      provides: "Custom domain declaration for GitHub Pages"
      contains: "sertaoseracloud.com"
    - path: "src/styles/global.css"
      provides: "ROADMAP-aligned CSS token alias comment block"
      contains: "--color-text-primary"
  key_links:
    - from: ".github/workflows/deploy.yml"
      to: "dist/"
      via: "pnpm build → actions/upload-pages-artifact@v3"
      pattern: "upload-pages-artifact"
    - from: "public/CNAME"
      to: "sertaoseracloud.com"
      via: "GitHub Pages custom domain detection"
      pattern: "sertaoseracloud\\.com"
    - from: "src/styles/global.css"
      to: "ROADMAP success criterion #4"
      via: "alias comment block documenting name mapping"
      pattern: "--color-text-primary"
---

<objective>
Close the two remaining Phase 1 gaps identified in VERIFICATION.md and HUMAN-UAT.md:

1. **Gap 1 (BLOCKER):** No `.github/workflows/deploy.yml` and no `public/CNAME` — the site cannot reach GitHub Pages. Create both files so pushing to `main` triggers a build-and-deploy.

2. **Gap 2 (PARTIAL):** ROADMAP Phase 1 success criterion names `--color-text-primary`, `--color-accent`, `--color-decorative` but the implemented Código Chama Azul design system uses its own naming convention. Add a brief alias comment block in `global.css` so the ROADMAP criterion can be verified without renaming any real token.

Purpose: After this plan runs and the user completes the authorial actions in `user_setup`, `https://sertaoseracloud.com` will return HTTP 200 with valid SSL, and all Phase 1 ROADMAP success criteria will be verifiable.

Output:
- `.github/workflows/deploy.yml` — complete, copy-pasteable workflow YAML
- `public/CNAME` — one-line file with literal `sertaoseracloud.com`
- `src/styles/global.css` — alias comment block added after the `:root` token section
</objective>

<execution_context>
@C:/Repo/blog_sertao/.claude/get-shit-done/workflows/execute-plan.md
@C:/Repo/blog_sertao/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-bootstrap-fundacoes/01-CONTEXT.md
@.planning/phases/01-bootstrap-fundacoes/01-VERIFICATION.md
@.planning/phases/01-bootstrap-fundacoes/01-HUMAN-UAT.md

<interfaces>
<!-- Decisions governing this plan -->
<!-- D-01: GitHub Pages via Actions-native workflow (actions/deploy-pages@v4, not branch gh-pages). Build on ubuntu-latest Node 22, publishes dist/. -->
<!-- D-02: Apex canonical domain: sertaoseracloud.com -->
<!-- D-03: CNAME file in repo with literal content sertaoseracloud.com (no https, no trailing newline) -->
<!-- D-04: Workflow uses actions/configure-pages@v5, actions/upload-pages-artifact@v3, actions/deploy-pages@v4. Single job: build-and-deploy. Trigger: push on main + workflow_dispatch. -->
<!-- D-22: pnpm is mandatory. The workflow must use pnpm (not npm). -->
<!-- D-23: Node 22.12+ LTS. Workflow must pin node-version: '22'. -->

<!-- astro.config.mjs already has: site: 'https://sertaoseracloud.com', no base path (correct for custom domain). -->
<!-- package.json already has: "build": "astro build" — used verbatim in workflow. -->
<!-- package.json already has: "packageManager": "pnpm@9.15.0" — workflow must match. -->

<!-- global.css token mapping (for alias block):             -->
<!--   --texto-principal  → --color-text-primary (body text) -->
<!--   --nucleo-eletrico  → --color-accent (primary accent)  -->
<!--   --chama-primaria   → --color-decorative (decorative)  -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create GitHub Actions deploy workflow and CNAME file</name>
  <files>.github/workflows/deploy.yml, public/CNAME</files>
  <read_first>
    - .planning/phases/01-bootstrap-fundacoes/01-CONTEXT.md (D-01, D-02, D-03, D-04, D-22, D-23 — all deploy decisions)
    - package.json (verify "build" script name and pnpm version in "packageManager" field)
    - astro.config.mjs (confirm site URL; no base path needed for custom domain)
  </read_first>
  <action>
    **Step 1 — Create `.github/workflows/deploy.yml`**

    First create the directory: `mkdir -p .github/workflows`

    Then write `.github/workflows/deploy.yml` with EXACTLY this content (per D-01/D-04):

    ```yaml
    name: Deploy to GitHub Pages

    on:
      push:
        branches: [main]
      workflow_dispatch:

    permissions:
      contents: read
      pages: write
      id-token: write

    concurrency:
      group: pages
      cancel-in-progress: false

    jobs:
      build-and-deploy:
        runs-on: ubuntu-latest
        environment:
          name: github-pages
          url: ${{ steps.deployment.outputs.page_url }}
        steps:
          - name: Checkout
            uses: actions/checkout@v4

          - name: Set up pnpm
            uses: pnpm/action-setup@v4
            with:
              version: 9.15.0

          - name: Set up Node.js
            uses: actions/setup-node@v4
            with:
              node-version: '22'
              cache: 'pnpm'

          - name: Install dependencies
            run: pnpm install --frozen-lockfile

          - name: Configure Pages
            uses: actions/configure-pages@v5

          - name: Build
            run: pnpm build

          - name: Upload artifact
            uses: actions/upload-pages-artifact@v3
            with:
              path: dist

          - name: Deploy to GitHub Pages
            id: deployment
            uses: actions/deploy-pages@v4
    ```

    Key decisions encoded in this YAML:
    - `pnpm/action-setup@v4` with version `9.15.0` matches `packageManager` in `package.json` (D-22)
    - `node-version: '22'` matches D-23 (Node 22)
    - `cache: 'pnpm'` speeds up subsequent runs
    - `pnpm install --frozen-lockfile` uses the committed `pnpm-lock.yaml` deterministically
    - `actions/configure-pages@v5` must run BEFORE `pnpm build` (sets `SITE` env var that Astro uses) — D-04
    - `path: dist` matches Astro's default `outDir` — D-01
    - Single job `build-and-deploy` — D-04
    - Triggers: push on `main` + `workflow_dispatch` — D-04
    - Permissions `pages: write` + `id-token: write` are required by `actions/deploy-pages` — D-01
    - `concurrency: group: pages, cancel-in-progress: false` prevents concurrent deploys — D-04

    Do NOT add `base` to `astro.config.mjs` — the custom domain `sertaoseracloud.com` means the site lives at the root, not a subpath. `astro.config.mjs` already has `site: 'https://sertaoseracloud.com'` which is correct.

    **Step 2 — Create `public/CNAME`**

    Write `public/CNAME` with EXACTLY this content and NO trailing newline (per D-03):

    ```
    sertaoseracloud.com
    ```

    Critical: no `https://`, no `www.`, no trailing slash, no trailing newline. The file must contain exactly 20 bytes: the ASCII string `sertaoseracloud.com`.

    GitHub Pages reads this file from the deployed `dist/CNAME` (Astro copies `public/` to `dist/` during build). Do NOT place it in `.github/` or the repo root.
  </action>
  <verify>
    <automated>test -f .github/workflows/deploy.yml && grep -q "actions/deploy-pages@v4" .github/workflows/deploy.yml && grep -q "pnpm/action-setup" .github/workflows/deploy.yml && grep -q "node-version: '22'" .github/workflows/deploy.yml && grep -q "cancel-in-progress: false" .github/workflows/deploy.yml && grep -q "contents: read" .github/workflows/deploy.yml && grep -q "pages: write" .github/workflows/deploy.yml && grep -q "id-token: write" .github/workflows/deploy.yml && grep -q "workflow_dispatch" .github/workflows/deploy.yml && test -f public/CNAME && grep -q "sertaoseracloud.com" public/CNAME</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/deploy.yml` exists
    - `.github/workflows/deploy.yml` contains `actions/deploy-pages@v4`
    - `.github/workflows/deploy.yml` contains `actions/configure-pages@v5`
    - `.github/workflows/deploy.yml` contains `actions/upload-pages-artifact@v3`
    - `.github/workflows/deploy.yml` contains `pnpm/action-setup`
    - `.github/workflows/deploy.yml` contains `node-version: '22'`
    - `.github/workflows/deploy.yml` contains `cancel-in-progress: false`
    - `.github/workflows/deploy.yml` contains `id-token: write` under `permissions:`
    - `.github/workflows/deploy.yml` contains `workflow_dispatch` under `on:`
    - `.github/workflows/deploy.yml` contains `path: dist` (not `path: ./dist`)
    - `public/CNAME` exists
    - `public/CNAME` contains exactly the string `sertaoseracloud.com` with no `https://` prefix
    - `cat public/CNAME` outputs `sertaoseracloud.com` with no extra lines
  </acceptance_criteria>
  <done>
    Workflow file and CNAME are in place. Pushing to main will trigger GitHub Actions. Once the user enables GitHub Pages (Settings > Pages > Source: GitHub Actions) and configures DNS A records (see user_setup), the site will be live at https://sertaoseracloud.com.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add ROADMAP alias comment block to global.css</name>
  <files>src/styles/global.css</files>
  <read_first>
    - src/styles/global.css (read the full :root block — specifically lines 64–112 — to find the exact insertion point after the closing brace of :root)
    - .planning/ROADMAP.md §"Phase 1 success criteria" (the exact token names cited: --color-text-primary, --color-accent, --color-decorative)
    - .planning/phases/01-bootstrap-fundacoes/01-VERIFICATION.md §"Gap 2" (the specific tokens named in the gap and their Código Chama Azul equivalents)
  </read_first>
  <action>
    The VERIFICATION.md Gap 2 states that ROADMAP example names `--color-text-primary`, `--color-accent`, `--color-decorative` are not present, but the implemented design system uses Código Chama Azul naming convention.

    Resolution: add a comment block + 3 thin CSS alias declarations AFTER the closing `}` of the `:root` block (around line 112 in the current file), BEFORE the `/* 04 · Reset & Base */` section.

    The alias block must contain:

    ```css
    /* ------------------------------------------------------------------ */
    /* 03b · ROADMAP Compatibility Aliases                                 */
    /*                                                                     */
    /* The ROADMAP Phase 1 success criterion names --color-text-primary,   */
    /* --color-accent, and --color-decorative as example token names.      */
    /* The Código Chama Azul design system supersedes those generic names  */
    /* (per ROADMAP Threat Model: "Design system supersedes Sertão brand   */
    /* colors from early planning"). These aliases satisfy the ROADMAP     */
    /* criterion without renaming any design-system token.                 */
    /*                                                                     */
    /* DO NOT use these aliases in components — use the canonical names.   */
    /* ------------------------------------------------------------------ */

    :root {
      --color-text-primary:  var(--texto-principal);   /* #FFFFFF — body text */
      --color-accent:        var(--nucleo-eletrico);    /* #00FFFF — accent (icons ≥24px, headings, borders) */
      --color-decorative:    var(--chama-primaria);     /* #1A3AC8 — decorative backgrounds, illustrations */
    }
    ```

    Insert this block between the closing `}` of the main `:root` block and the `/* 04 · Reset & Base */` comment. Do NOT modify any existing token name or value. Do NOT use these aliases anywhere in the component files — they exist only for ROADMAP criterion verification.

    Use the Edit tool to insert at the correct line. The `:root` block closes with `}` at approximately line 112 (after `--scanline-opacity: 0.06;`). Insert the new block immediately after that `}`.
  </action>
  <verify>
    <automated>grep -q "\-\-color-text-primary" src/styles/global.css && grep -q "\-\-color-accent" src/styles/global.css && grep -q "\-\-color-decorative" src/styles/global.css && grep -q "var(--texto-principal)" src/styles/global.css && grep -q "var(--nucleo-eletrico)" src/styles/global.css && grep -q "var(--chama-primaria)" src/styles/global.css && grep -q "ROADMAP Compatibility Aliases" src/styles/global.css</automated>
  </verify>
  <acceptance_criteria>
    - `src/styles/global.css` contains `--color-text-primary`
    - `src/styles/global.css` contains `--color-accent`
    - `src/styles/global.css` contains `--color-decorative`
    - `src/styles/global.css` contains `var(--texto-principal)` as the value of `--color-text-primary`
    - `src/styles/global.css` contains `var(--nucleo-eletrico)` as the value of `--color-accent`
    - `src/styles/global.css` contains `var(--chama-primaria)` as the value of `--color-decorative`
    - `src/styles/global.css` contains the comment `ROADMAP Compatibility Aliases`
    - No existing token in `:root` was renamed or removed (run `grep -c "nucleo-eletrico" src/styles/global.css` — must return ≥2, one in @theme and one in :root)
    - `pnpm build` exits 0 (no CSS parse error introduced)
  </acceptance_criteria>
  <done>
    ROADMAP Phase 1 success criterion "Paleta semântica em CSS vars (--color-text-primary, --color-accent, --color-decorative)" is now verifiable by grep. No design-system token was renamed. Gap 2 is closed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions runner → dist/ | Build output is generated from committed source; no external inputs at build time |
| DNS → GitHub Pages IPs | User controls DNS; A records point to GitHub's servers (not ours); GitHub validates CNAME ownership |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03-01 | Tampering | `.github/workflows/deploy.yml` | mitigate | Use `actions/checkout@v4` (pinned major) + `--frozen-lockfile` to prevent supply-chain substitution; no `GITHUB_TOKEN` write beyond Pages scope |
| T-01-03-02 | Elevation of Privilege | Workflow permissions | mitigate | `permissions: contents: read` limits checkout to read-only; `pages: write` is scoped only to the Pages environment; `id-token: write` limited to OIDC token for Pages deployment |
| T-01-03-03 | Denial of Service | concurrency group | accept | `cancel-in-progress: false` prevents mid-deploy cancellation; back-to-back pushes queue rather than cancel — acceptable for a single-author blog |
| T-01-03-04 | Information Disclosure | public/CNAME | accept | CNAME discloses the custom domain name, which is already public information by design |
| T-01-03-05 | Spoofing | DNS A records | transfer | DNS record accuracy is the user's responsibility at their registrar; GitHub Pages validates domain ownership via CNAME file |
</threat_model>

<verification>
After both tasks complete:

1. `test -f .github/workflows/deploy.yml` — exits 0
2. `grep -q "actions/deploy-pages@v4" .github/workflows/deploy.yml` — exits 0
3. `test -f public/CNAME && grep -q "sertaoseracloud.com" public/CNAME` — exits 0
4. `grep -q "\-\-color-text-primary" src/styles/global.css` — exits 0
5. `grep -q "\-\-color-accent" src/styles/global.css` — exits 0
6. `grep -q "\-\-color-decorative" src/styles/global.css` — exits 0
7. `pnpm build` — exits 0, `dist/CNAME` exists in output
8. `grep -q "sertaoseracloud.com" dist/CNAME` — exits 0 (confirms Astro copies public/ to dist/)

User authorial actions required before live verification:
- Enable GitHub Pages in repo Settings > Pages > Source: GitHub Actions
- Add 4 DNS A records at registrar (see user_setup)
- Push a commit to `main` and confirm GitHub Actions workflow succeeds
- After DNS propagation (up to 24h): `curl -I https://sertaoseracloud.com` returns `HTTP/2 200`
</verification>

<success_criteria>
- VERIFICATION.md Gap 1 CLOSED: `.github/workflows/deploy.yml` exists with correct Actions (deploy-pages@v4, configure-pages@v5, upload-pages-artifact@v3), pnpm setup, Node 22, correct permissions, and concurrency config.
- VERIFICATION.md Gap 2 CLOSED: `src/styles/global.css` contains `--color-text-primary`, `--color-accent`, `--color-decorative` as ROADMAP-compatible aliases mapping to Código Chama Azul canonical tokens.
- UAT Test 3 unblocked (code side): after user enables GitHub Pages and configures DNS, the workflow will deploy the site to `https://sertaoseracloud.com`.
- ROADMAP Phase 1 success criterion "sertaoseracloud.com retorna HTTP 200 com SSL válido via Github Pages" will be achievable once the user completes user_setup steps.
- ROADMAP Phase 1 success criterion "Paleta semântica em CSS vars (--color-text-primary, --color-accent, --color-decorative)" is now grep-verifiable.
- No existing design-system token was renamed; `pnpm build` continues to exit 0.
</success_criteria>

<output>
After completion, create `.planning/phases/01-bootstrap-fundacoes/01-03-SUMMARY.md` documenting:
- Exact content of `.github/workflows/deploy.yml` written (confirm pnpm version + Node version used)
- Confirmation that `public/CNAME` contains `sertaoseracloud.com` with no trailing newline
- Confirmation that `pnpm build` exits 0 after `global.css` edit and that `dist/CNAME` exists
- The 3 CSS alias mappings added and their purpose
- What the user must do next (the authorial user_setup steps) to make the site go live
- Any deviation from the planned workflow YAML (e.g., if a specific action version was unavailable)
</output>
