---
phase: 07-newsletter-lgpd
plan: 02
type: execute
wave: 2
depends_on:
  - "07-01"
files_modified:
  - src/pages/newsletter.astro
  - src/pages/privacidade.astro
  - src/components/Footer.astro
autonomous: true
requirements:
  - D-04
  - D-06
  - D-07
  - D-08
  - D-09
  - D-10
  - D-11
  - D-12

must_haves:
  truths:
    - "A /newsletter page exists with the D-07 pitch copy and a working subscribe form with LGPD checkbox"
    - "/privacidade names Buttondown Email, Inc. as the email sub-processor with a link to their privacy policy"
    - "/privacidade contains all seven required LGPD disclosure items (finalidade, base legal, dados coletados, sub-processador, retenção, direitos Art. 18, mecanismo revogação)"
    - "The /privacidade 'não coleta dados pessoais' claim is narrowed to the analytics/tracking context so it remains accurate after newsletter launch"
    - "The Footer contains a link to /newsletter"
    - "The /privacidade last-updated date reflects Phase 7 implementation (2026-04-28)"
  artifacts:
    - path: "src/pages/newsletter.astro"
      provides: "Standalone newsletter subscribe page with pitch + form"
      contains: "Receba novos posts sobre cloud"
    - path: "src/pages/privacidade.astro"
      provides: "Full LGPD-compliant privacy policy including newsletter email section"
      contains: "Buttondown"
    - path: "src/components/Footer.astro"
      provides: "Footer with Newsletter nav link"
      contains: "/newsletter"
  key_links:
    - from: "src/pages/newsletter.astro"
      to: "https://buttondown.com/api/emails/embed-subscribe/REPLACE_WITH_BUTTONDOWN_USERNAME"
      via: "form action + is:inline script (same pattern as NewsletterEmbed.astro)"
      pattern: "no-cors"
    - from: "src/pages/privacidade.astro"
      to: "https://buttondown.com/legal/privacy"
      via: "anchor link within Buttondown sub-processor prose"
      pattern: "buttondown.com/legal/privacy"

user_setup:
  - service: buttondown
    why: "RSS-to-email integration — no code change required, authorial dashboard config only"
    env_vars: []
    dashboard_config:
      - task: "Enable RSS-to-email: Buttondown dashboard → Sending → RSS-to-email → add feed URL https://sertaoseracloud.com/rss.xml"
        location: "Buttondown dashboard → Sending → RSS-to-email"
      - task: "Replace REPLACE_WITH_BUTTONDOWN_USERNAME in src/pages/newsletter.astro (form action + USERNAME var in is:inline script)"
        location: "src/pages/newsletter.astro — two occurrences"
---

<objective>
Create the standalone /newsletter page, update /privacidade with the complete LGPD newsletter disclosure, and add a Newsletter footer link.

Purpose: Completes Phase 7 deliverables — subscriber acquisition page (D-06, D-07), LGPD compliance disclosure (D-09 through D-12), and site navigation update. D-04 and D-08 are authorial Buttondown dashboard actions documented in user_setup (no code change required).

Output: newsletter.astro, privacidade.astro updated with newsletter LGPD section, Footer.astro updated with /newsletter link.
</objective>

<execution_context>
@C:/Repo/blog_sertao/.claude/get-shit-done/workflows/execute-plan.md
@C:/Repo/blog_sertao/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Repo/blog_sertao/.planning/PROJECT.md
@C:/Repo/blog_sertao/.planning/ROADMAP.md
@C:/Repo/blog_sertao/.planning/STATE.md
@C:/Repo/blog_sertao/.planning/phases/07-newsletter-lgpd/07-CONTEXT.md
@C:/Repo/blog_sertao/.planning/phases/07-newsletter-lgpd/07-RESEARCH.md
@C:/Repo/blog_sertao/.planning/phases/07-newsletter-lgpd/07-UI-SPEC.md
@C:/Repo/blog_sertao/.planning/phases/07-newsletter-lgpd/07-01-SUMMARY.md

<interfaces>
<!-- Key contracts and existing code the executor needs. -->

From src/pages/privacidade.astro (existing file, read before editing):
- Line 7-10: BaseLayout call with description prop — MUST update description to reflect email collection
- Line 17: "Última atualização: 25 de abril de 2026" — update date to "28 de abril de 2026"
- Lines 28-32: "Dados Pessoais" section — MUST update to narrow the "não coleta dados pessoais" claim
- Lines 35-40: "Contato" section — must remain, new newsletter section goes BEFORE this section
- Existing structure: h2 "Rastreamento e Cookies" → h2 "Dados Pessoais" → h2 "Newsletter e Email" (NEW) → h2 "Contato" → h2 "Atualizações"

From src/components/Footer.astro (existing file, read before editing):
- const socials array at lines 4-8: { href, label } objects
- Current entries: dev.to, GitHub, RSS
- Add: { href: '/newsletter', label: 'Newsletter' } to this array

From src/pages/tags/index.astro (layout pattern for /newsletter):
- Uses BaseLayout + Header + Footer + <div class="stage">
- h1 with clamp(28px, 2.8vw, 36px) size — follow this heading pattern

LGPD Art. 18 disclosure prose (exact text, from 07-RESEARCH.md):
The new h2 "Newsletter e Email" section in /privacidade must contain these items as a <ul>:
  - Finalidade: envio de novos posts e atualizações editoriais
  - Base legal: consentimento do titular (Art. 7º, I, LGPD)
  - Dados coletados: endereço de e-mail
  - Sub-processador: Buttondown Email, Inc. (EUA) — link to https://buttondown.com/legal/privacy
  - Tempo de retenção: enquanto a assinatura estiver ativa + 30 dias após cancelamento para permitir reinscrição e processamento de opt-out
  - Direitos do titular (Art. 18 LGPD): confirmar existência, acessar, corrigir, eliminar, portar dados e revogar consentimento — via cancelamento (link em todo email) ou contato em engcfraposo@gmail.com
  - Mecanismo de revogação: link de cancelamento (unsubscribe) presente em todo email enviado pelo Buttondown

Copywriting contract from 07-UI-SPEC.md:
  - /newsletter page title: "Newsletter · O Sertão será Cloud"
  - /newsletter meta description: "Assine a newsletter para receber novos posts sobre cloud, DevOps e arquitetura diretamente no email. Sem spam."
  - Section heading (h1): "Newsletter"
  - Pitch paragraph (D-07 locked): "Receba novos posts sobre cloud, DevOps e arquitetura diretamente no seu email. Posts novos chegam automaticamente — sem spam, cancele quando quiser."
  - CTA button: "Inscrever-se"
  - LGPD checkbox label: "Li e aceito a [Política de Privacidade](/privacidade) e consinto com o envio de emails."
  - Success message: "Obrigado! Confira sua caixa de entrada para confirmar."
  - Error message (network): "Algo deu errado. Tente novamente ou entre em contato: engcfraposo@gmail.com"
  - /privacidade new section heading: "Newsletter e Email"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create /newsletter standalone page</name>
  <files>src/pages/newsletter.astro</files>

  <read_first>
    - src/pages/tags/index.astro — read to confirm stage layout, h1 sizing pattern, BaseLayout import path
    - src/components/NewsletterEmbed.astro — read to confirm the exact form/script markup to duplicate here
  </read_first>

  <action>
Create `src/pages/newsletter.astro` as a new file. The page duplicates the form markup and is:inline script from NewsletterEmbed.astro — this is intentional per 07-UI-SPEC.md to keep the is:inline script co-located with its markup and avoid prop drilling. Both occurrences use the same REPLACE_WITH_BUTTONDOWN_USERNAME placeholder.

Write the file with the following content:

```astro
---
import BaseLayout from '../layouts/BaseLayout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
---

<BaseLayout
  title="Newsletter · O Sertão será Cloud"
  description="Assine a newsletter para receber novos posts sobre cloud, DevOps e arquitetura diretamente no email. Sem spam."
>
  <Header slot="header" />

  <div class="stage">
    <section style="padding:64px 0 32px; max-width:720px;">
      <h1
        style="font-family:'Chakra Petch',sans-serif;font-size:clamp(28px,2.8vw,36px);font-weight:600;line-height:1.2;color:var(--texto-principal);margin-bottom:var(--space-5);"
      >
        Newsletter
      </h1>
      <p
        style="font-family:'Space Grotesk',system-ui,sans-serif;font-size:18px;line-height:1.72;color:var(--texto-principal);max-width:540px;"
      >
        Receba novos posts sobre cloud, DevOps e arquitetura diretamente no seu email.
        Posts novos chegam automaticamente — sem spam, cancele quando quiser.
      </p>
    </section>

    <section style="max-width:540px; padding-bottom:64px;">
      <form
        id="newsletter-form"
        action="https://buttondown.com/api/emails/embed-subscribe/REPLACE_WITH_BUTTONDOWN_USERNAME"
        method="POST"
        target="_blank"
      >
        <div class="newsletter-field-row">
          <label for="newsletter-email" class="sr-only">Endereço de email</label>
          <input
            type="email"
            id="newsletter-email"
            name="email"
            required
            placeholder="seu@email.com"
            aria-label="Endereço de email"
            autocomplete="email"
          />
          <button type="submit" class="newsletter-submit-btn">
            Inscrever-se
          </button>
        </div>

        <div class="newsletter-consent">
          <input
            type="checkbox"
            id="newsletter-consent"
            name="consent"
            required
          />
          <label for="newsletter-consent">
            Li e aceito a <a href="/privacidade">Política de Privacidade</a>
            e consinto com o envio de emails.
          </label>
        </div>
      </form>

      <div
        id="newsletter-success"
        hidden
        role="status"
        tabindex="-1"
        class="newsletter-message newsletter-message--success"
      >
        Obrigado! Confira sua caixa de entrada para confirmar.
      </div>

      <div
        id="newsletter-error"
        hidden
        role="alert"
        tabindex="-1"
        class="newsletter-message newsletter-message--error"
      >
      </div>
    </section>
  </div>

  <Footer slot="footer" />
</BaseLayout>

<script is:inline>
  (function () {
    var USERNAME = 'REPLACE_WITH_BUTTONDOWN_USERNAME';
    var form = document.getElementById('newsletter-form');
    var successDiv = document.getElementById('newsletter-success');
    var errorDiv = document.getElementById('newsletter-error');
    if (!form) return;
    var submitBtn = form.querySelector('button[type="submit"]');

    form.addEventListener('submit', function (e) {
      e.preventDefault();
      var emailVal = document.getElementById('newsletter-email').value;

      submitBtn.disabled = true;
      submitBtn.textContent = 'Enviando...';
      errorDiv.setAttribute('hidden', '');

      var body = new URLSearchParams();
      body.append('email', emailVal);

      fetch(
        'https://buttondown.com/api/emails/embed-subscribe/' + USERNAME,
        { method: 'POST', body: body, mode: 'no-cors' }
      )
        .then(function () {
          form.style.display = 'none';
          successDiv.removeAttribute('hidden');
          successDiv.focus();
        })
        .catch(function () {
          submitBtn.disabled = false;
          submitBtn.textContent = 'Inscrever-se';
          errorDiv.textContent =
            'Algo deu errado. Tente novamente ou entre em contato: engcfraposo@gmail.com';
          errorDiv.removeAttribute('hidden');
          errorDiv.focus();
        });
    });
  })();
</script>
```

Do NOT add `client:` directive. Do NOT pre-check the consent checkbox.
  </action>

  <verify>
    <automated>grep -c "Receba novos posts\|REPLACE_WITH_BUTTONDOWN_USERNAME\|no-cors\|newsletter-consent" C:/Repo/blog_sertao/src/pages/newsletter.astro</automated>
  </verify>

  <acceptance_criteria>
    - File `src/pages/newsletter.astro` exists
    - `grep "Receba novos posts sobre cloud" src/pages/newsletter.astro` returns a match (D-07 pitch copy present)
    - `grep "REPLACE_WITH_BUTTONDOWN_USERNAME" src/pages/newsletter.astro` returns 2 matches (form action + USERNAME var)
    - `grep "no-cors" src/pages/newsletter.astro` returns 1 match
    - `grep "checked" src/pages/newsletter.astro` returns NO match (checkbox not pre-checked)
    - `grep 'tabindex="-1"' src/pages/newsletter.astro` returns 2 matches (success and error divs)
    - `grep "Newsletter · O Sertão será Cloud" src/pages/newsletter.astro` returns 1 match (page title)
    - `pnpm astro check` exits with no errors
    - `pnpm build` succeeds and `dist/newsletter/index.html` exists
  </acceptance_criteria>

  <done>/newsletter page created with pitch copy, form, LGPD checkbox, and AJAX handler; pnpm build produces dist/newsletter/index.html.</done>
</task>

<task type="auto">
  <name>Task 2: Update /privacidade with LGPD newsletter section and fix personal data claim</name>
  <files>src/pages/privacidade.astro</files>

  <read_first>
    - src/pages/privacidade.astro — read in full (54 lines) to understand current structure before editing
  </read_first>

  <action>
Edit `src/pages/privacidade.astro` with the following changes — make all four changes in a single write:

**Change 1: Update BaseLayout description prop** (line 9)
Replace:
```
description="Política de privacidade do blog O Sertão será Cloud. Este site não utiliza cookies de rastreamento nem coleta dados pessoais."
```
With:
```
description="Política de privacidade do blog O Sertão será Cloud. Sem cookies de rastreamento. Email coletado apenas ao assinar a newsletter voluntariamente."
```

**Change 2: Update last-updated date** (line 17)
Replace:
```
Última atualização: 25 de abril de 2026
```
With:
```
Última atualização: 28 de abril de 2026
```

**Change 3: Update "Dados Pessoais" section** (lines 28-32, D-12)
Replace the existing paragraph under h2 "Dados Pessoais":
```html
<p>
  Este site não coleta dados pessoais de visitantes. Não há formulários de cadastro,
  comentários ou interações que requeiram identificação. Caso analytics sejam adicionados
  no futuro, esta página será atualizada antes da ativação.
</p>
```
With:
```html
<p>
  Este site não utiliza cookies de rastreamento nem coleta dados de navegação dos visitantes.
  O único dado pessoal coletado é o endereço de e-mail, e apenas quando o visitante assina
  a newsletter voluntariamente. Veja a seção <a href="#newsletter">Newsletter e Email</a> abaixo.
</p>
```

**Change 4: Insert "Newsletter e Email" section** between the "Dados Pessoais" section and the "Contato" section:
```html
      <h2 id="newsletter">Newsletter e Email</h2>
      <p>
        Ao assinar a newsletter voluntariamente, coletamos e processamos seu endereço de e-mail
        com a finalidade de enviar novos posts e atualizações editoriais deste blog.
      </p>
      <ul>
        <li><strong>Finalidade:</strong> envio de novos posts e atualizações editoriais.</li>
        <li><strong>Base legal:</strong> consentimento do titular (Art. 7º, I, LGPD).</li>
        <li><strong>Dados coletados:</strong> endereço de e-mail.</li>
        <li>
          <strong>Sub-processador:</strong> Buttondown Email, Inc. (EUA) —
          <a href="https://buttondown.com/legal/privacy" target="_blank" rel="noopener noreferrer">política de privacidade do Buttondown</a>.
          Ao assinar, você consente com a transferência internacional do seu e-mail
          aos servidores da Buttondown localizados nos Estados Unidos (Art. 33, VIII, LGPD).
        </li>
        <li>
          <strong>Tempo de retenção:</strong> enquanto a assinatura estiver ativa + 30 dias
          após cancelamento para permitir reinscrição e processamento de opt-out.
        </li>
        <li>
          <strong>Direitos do titular (Art. 18 LGPD):</strong> você tem o direito de confirmar
          a existência de tratamento, acessar, corrigir, eliminar, portar seus dados e revogar
          o consentimento a qualquer momento. Para exercer esses direitos: cancele a assinatura
          pelo link em todo e-mail recebido, ou entre em contato em
          <a href="mailto:engcfraposo@gmail.com">engcfraposo@gmail.com</a>.
        </li>
        <li>
          <strong>Mecanismo de revogação:</strong> link de cancelamento (unsubscribe) presente
          em todo e-mail enviado pelo Buttondown.
        </li>
      </ul>
```

The final section order in the file must be:
h2 "Rastreamento e Cookies" → h2 "Dados Pessoais" (updated) → h2 "Newsletter e Email" (new, id="newsletter") → h2 "Contato" (unchanged) → h2 "Atualizações" (unchanged)
  </action>

  <verify>
    <automated>grep -c "Buttondown\|Art\. 18\|30 dias\|retenção\|28 de abril de 2026" C:/Repo/blog_sertao/src/pages/privacidade.astro</automated>
  </verify>

  <acceptance_criteria>
    - `grep "28 de abril de 2026" src/pages/privacidade.astro` returns 1 match (updated date)
    - `grep "Buttondown" src/pages/privacidade.astro` returns at least 2 matches (prose mention + policy link)
    - `grep "Art\. 18" src/pages/privacidade.astro` returns at least 1 match
    - `grep "30 dias" src/pages/privacidade.astro` returns 1 match (retention period, D-10)
    - `grep "Art\. 7" src/pages/privacidade.astro` returns 1 match (legal basis)
    - `grep "Art\. 33" src/pages/privacidade.astro` returns 1 match (international transfer)
    - `grep "buttondown.com/legal/privacy" src/pages/privacidade.astro` returns 1 match (D-09 sub-processor link)
    - `grep "não coleta dados pessoais de visitantes" src/pages/privacidade.astro` returns NO match (old inaccurate claim removed, D-12)
    - `grep 'id="newsletter"' src/pages/privacidade.astro` returns 1 match (anchor for in-page link)
    - `grep "Assine a newsletter" src/pages/privacidade.astro` returns NO match (description is in BaseLayout call, not content — verify description prop was updated)
    - `grep "Email coletado apenas ao assinar" src/pages/privacidade.astro` returns 1 match (updated BaseLayout description)
    - `pnpm astro check` exits with no errors
    - `pnpm build && grep "Buttondown" dist/privacidade/index.html` returns matches (Buttondown appears in built output)
  </acceptance_criteria>

  <done>/privacidade updated with full LGPD newsletter section, corrected personal data claim, updated date, and updated meta description; build succeeds.</done>
</task>

<task type="auto">
  <name>Task 3: Add Newsletter link to Footer</name>
  <files>src/components/Footer.astro</files>

  <read_first>
    - src/components/Footer.astro — read in full (31 lines) to confirm the socials array structure
  </read_first>

  <action>
Edit `src/components/Footer.astro` to add `{ href: '/newsletter', label: 'Newsletter' }` to the `socials` array. Insert it as the first entry (before dev.to) so the footer reads: Newsletter · dev.to · GitHub · RSS. This keeps newsletter discovery prominent without cluttering the main header nav.

Change the socials array from:
```javascript
const socials = [
  { href: 'https://dev.to/sertaoseracloud', label: 'dev.to' },
  { href: 'https://github.com/sertaoseracloud', label: 'GitHub' },
  { href: '/rss.xml', label: 'RSS' },
];
```
To:
```javascript
const socials = [
  { href: '/newsletter', label: 'Newsletter' },
  { href: 'https://dev.to/sertaoseracloud', label: 'dev.to' },
  { href: 'https://github.com/sertaoseracloud', label: 'GitHub' },
  { href: '/rss.xml', label: 'RSS' },
];
```

No other changes to Footer.astro. The existing template renders all socials identically (the `href.startsWith('http')` check already handles relative vs absolute correctly — `/newsletter` does not start with `http`, so no `target="_blank"` will be added, which is correct behavior for an internal page).
  </action>

  <verify>
    <automated>grep -n "newsletter\|socials" C:/Repo/blog_sertao/src/components/Footer.astro</automated>
  </verify>

  <acceptance_criteria>
    - `grep "newsletter" src/components/Footer.astro` returns at least 1 match
    - `grep "'/newsletter'" src/components/Footer.astro` returns 1 match (internal href, no target blank)
    - The newsletter entry appears before dev.to in the socials array (check line numbers via grep -n)
    - `pnpm astro check` exits with no errors
    - `pnpm build && grep "/newsletter" dist/index.html` returns a match (newsletter link present in built homepage footer)
  </acceptance_criteria>

  <done>Footer.astro socials array includes Newsletter as first entry; link renders in built output.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
All Phase 7 code is complete:
- Plan 01: global.css Phase 7 CSS block + NewsletterEmbed.astro component + PostLayout wired
- Plan 02: /newsletter page + /privacidade LGPD update + Footer newsletter link

Claude has done all implementation. This checkpoint verifies the result visually and confirms the Buttondown smoke test readiness.
  </what-built>

  <how-to-verify>
1. Run `pnpm build` — must exit green with no errors.
2. Run `pnpm preview` and open http://localhost:4321 in a browser.
3. Navigate to any post (e.g., /posts/{your-slug}).
   - Confirm the "Receba novos posts" section appears between the article and the Comentários section.
   - Confirm the email input and "Inscrever-se" button are visible.
   - Confirm the LGPD consent checkbox is NOT pre-checked.
4. Navigate to http://localhost:4321/newsletter.
   - Confirm "Newsletter" h1 is visible.
   - Confirm pitch copy: "Receba novos posts sobre cloud, DevOps e arquitetura diretamente no seu email. Posts novos chegam automaticamente — sem spam, cancele quando quiser."
5. Navigate to http://localhost:4321/privacidade.
   - Confirm "Newsletter e Email" section is present with the Buttondown sub-processor link.
   - Confirm "Última atualização: 28 de abril de 2026" date.
   - Confirm the old "Este site não coleta dados pessoais de visitantes" phrase is gone.
6. Check the footer on any page — confirm "Newsletter" link appears in the footer nav.
7. (Optional, requires Buttondown account) If you have created the Buttondown account:
   - Replace REPLACE_WITH_BUTTONDOWN_USERNAME in both files.
   - Rerun `pnpm preview`, submit a test email in the form.
   - Confirm success message appears inline without page reload.
   - Check Buttondown dashboard for the pending subscriber.
  </how-to-verify>

  <resume-signal>Type "approved" if everything looks correct, or describe any visual or functional issues found.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| /newsletter form → Buttondown embed endpoint | User-supplied email; same trust model as NewsletterEmbed.astro |
| /privacidade static prose → reader | No user input; static content only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-06 | Information Disclosure | /privacidade LGPD disclosure completeness | mitigate | All seven LGPD items from D-11 must be present in the section; acceptance criteria grep-verifies six of them |
| T-07-07 | Tampering | /newsletter consent checkbox pre-check | mitigate | acceptance criteria grep "checked" must return NO match; `required` attribute enforces browser gate |
| T-07-08 | Tampering | XSS in /newsletter is:inline error copy | mitigate | `errorDiv.textContent = ...` (not innerHTML); email value never reflected in DOM |
| T-07-09 | Information Disclosure | Outdated /privacidade description claiming no personal data | mitigate | D-12: description prop updated to reflect email collection; acceptance criteria verifies old text removed |
</threat_model>

<verification>
After completing all three tasks and the checkpoint:

```bash
pnpm build
```
Must succeed — confirms all three new/updated files compile without errors.

```bash
grep "Buttondown\|Art\. 18\|30 dias" src/pages/privacidade.astro
```
Must return matches for all three patterns.

```bash
grep "Receba novos posts sobre cloud" src/pages/newsletter.astro
```
Must return 1 match (D-07 pitch copy present).

```bash
grep "/newsletter" src/components/Footer.astro
```
Must return 1 match (footer link present).

```bash
grep "não coleta dados pessoais de visitantes" src/pages/privacidade.astro
```
Must return NO match (inaccurate D-12 claim removed).

```bash
pnpm build && grep "Buttondown" dist/privacidade/index.html
```
Must return matches (Buttondown appears in built output — LGPD section rendered).
</verification>

<success_criteria>
- /newsletter page exists with locked pitch copy, form, LGPD checkbox, AJAX handler
- /privacidade contains all seven LGPD newsletter disclosure items (Art. 7 I, Art. 18, Art. 33, Buttondown sub-processor, retention period, revocation mechanism, contact)
- /privacidade "Dados Pessoais" section no longer claims the site collects zero personal data (D-12)
- /privacidade date updated to 28 de abril de 2026
- Footer includes /newsletter link
- `pnpm build` exits green
- Human checkpoint approved (visual + smoke test)
</success_criteria>

<output>
After completion, create `.planning/phases/07-newsletter-lgpd/07-02-SUMMARY.md` using the summary template.
</output>
